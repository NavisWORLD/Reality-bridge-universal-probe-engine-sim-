# Falsifiable hypotheses

H1. Identical recorded inputs and engine version reproduce the same discrete simulation event sequence within declared numerical tolerance.

H2. State channels intentionally wired to sensor input respond measurably while unrelated channels remain within declared coupling.

H3. APOD/USGS context can alter deterministic generation through authored software rules; that is a generation property, not evidence of physical causation.

H4. A physically assembled reference probe can meet preregistered latency, packet-loss and calibration thresholds.

H5. Memory Echo/replay can preserve declared world and event invariants across export/import.
